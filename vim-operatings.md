# vi/vim 操作

## Overview

## Operatings

### "Normal-mode", 正常模式, 由 { k[Esc] ? k[Esc] * 2 } 进入

### "Insert-mode", 插入模式 / 编辑模式,  由 { k[i] } 进入

### "Command-mode", 命令模式, 由 { k[:], k[/] } 进入

### "Visual-mode", 可视模式, 由 { k[v], k[V], k[Ctrl+v] } 进入